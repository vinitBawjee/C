#include <stdio.h>

// calculates and prints the volume of a sphere
int main() {
    int radius = 7;
    float volume = 4 * 3.14 * radius * radius * radius / 3;
    printf("Volume of a Sphere: %f\n", volume);
    return 0;
}
